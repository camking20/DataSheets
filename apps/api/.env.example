# API must use datasheets_runtime — never the container superuser or datasheets_owner.
# Dev password only; production must override via ALTER ROLE / secrets.
DATABASE_URL=postgresql://datasheets_runtime:datasheets_runtime@localhost:5432/datasheets
PORT=4000
